# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code（claude.ai/code）への指針を提供します。

## リポジトリの状態

このリポジトリは **仕様策定段階** です。実装はまだ存在しません。ソースコード、
`package.json` / `requirements.txt` などのパッケージマニフェスト、ビルドシステム、
テスト、CI のいずれもありません。現時点で正となる内容は以下の 2 つのみです。

- `project.md` — プロダクト仕様書（日本語）
- `sample/` — 入力 MP4 と目標出力 GIF のペア。変換結果の **視覚的な仕様** として扱う

ユーザーからビルド／テスト／lint コマンドを尋ねられたら、率直に「まだ存在しない」
と答えてください。スタックを勝手に決めて導入してはいけません。導入が必要な場合は
必ず事前に確認してください。

## プロダクト概要（`project.md` より）

**Monorize** は、ユーザーが選択した動画を **モノクロのアニメーション GIF** に
変換し、ダウンロードできる Web アプリです。**変換処理はすべてユーザーの
ブラウザ内で完結し、動画をサーバへ送信しません**（純粋な静的 SPA）。

### 厳守すべき制約

- **1 操作あたり 1 ファイル。** バッチ処理は行わない。
- **入力サイズの推奨上限：500 MB**（実際の上限はブラウザ／端末メモリに依存）
- **受け付ける入力形式：** `.mp4`, `.mov`, `.avi`, `.mkv`, `.webm`, `.wmv`,
  `.flv`, `.m4v`, `.ts`, `.3gp`
- **出力：** モノクロのアニメーション GIF。`sample/` にあるペア（例：
  `sample/MAH03703.MP4` → `sample/MAH03703.gif`）の見た目に揃える。サンプルが
  「モノクロのアニメーション GIF」とは何かを定義する仕様そのもの。
- **UI：** ファイル選択ボタン **と** ドラッグ＆ドロップの両方を動作させる。
  変換完了後にのみダウンロードボタンを表示する。
- **保持ポリシー：** サーバを持たないため自動削除処理は不要。動画・GIF は
  ブラウザのメモリ上にしか存在せず、タブクローズ等で自動的に消える。

### 完了条件（仕様書より）

1. ローカル開発環境での手動テストに合格する
2. ローカル自動化テストで **10,000 件** の変換を行い、生成された GIF の
   品質チェックに合格する（Playwright か Node + ffmpeg.wasm のいずれか）
3. XServer 本番環境および GitHub Pages ステージング環境で実機動作確認に
   合格する

## 使用言語

**すべての成果物を日本語で作成すること。** これは 2026-05-01 にユーザーから
明示された恒久的な指示です。対象は次のすべてを含みます。

- ユーザーへのチャット返信、進捗報告、ターン末の要約、`AskUserQuestion` の
  質問文・選択肢ラベル・説明文
- コード内のコメントおよびドキュメンテーションコメント
- コミットメッセージ、PR タイトル、PR 本文
- README、設計メモ、その他のドキュメント類

ただし以下は **英語のまま** とします（言語仕様・エコシステム慣習のため）。

- プログラミング言語のキーワード、標準ライブラリ／フレームワーク／パッケージの API 名
- 識別子（変数名・関数名・クラス名・ファイル名・ディレクトリ名など）
- 設定ファイルのキー名、環境変数名

ユーザーから個別に別言語での出力を求められた場合のみ、その範囲で切り替えます。

## サンプル素材

`sample/` には MP4 と GIF のペアが 6 組あります。ファイル名は対応しています
（`MAH03703.MP4` の入力に対して `MAH03703.gif` が期待される出力）。フレーム
レート、ディザリング、パレット、スケール、モノクロ処理などの変換パラメータを
調整する際は、これらを正解（ground truth）として用いてください。

## 確定済みの実装方針

実装着手前に確認が必要だった事項はすべて確定済みです。詳細は `project.md` を
正として参照してください。要点のみ以下に列挙します。

- **アーキテクチャ**：純粋な静的 SPA。バックエンドなし、ストレージなし。
  ユーザーのブラウザ内で変換まで完結する
- **フロントエンド**：Vite + React
- **変換エンジン**：[ffmpeg.wasm](https://ffmpegwasm.netlify.app/)
  （WebAssembly 版 ffmpeg）。フィルタは `scale=W:trunc(ow/dar/2)*2,hue=s=0`
  をそのまま使える
- **本番配信**：XServer（既存契約のレンタルサーバ）。既存ドメインの
  `/monorize/` サブパス配下に配置。`.htaccess` で COOP/COEP を設定し、
  ffmpeg.wasm のマルチスレッド版を有効化
- **ステージング配信**：GitHub Pages（無料）。COOP/COEP を設定できないため
  ffmpeg.wasm のシングルスレッド版を使用
- **保持ポリシー**：サーバを持たないため不要。ファイルはブラウザのメモリ
  上にしか存在せず、タブクローズ等で自動消滅
- **UI 入力レンジ**：解像度は元動画サイズが上限（％は 1〜100）、FPS は 1〜30 fps、
  出力サイズは事前推定＋警告のみ（既定閾値 200 MB、ハード上限なし）
- **完了条件**：10,000 件の変換品質チェックは **ローカル自動化** で実施
  （Playwright によるブラウザ自動化、または Node.js 上で ffmpeg.wasm を
  CLI 的に呼ぶ方式のいずれか。実装フェーズで決定）

新たな仕様判断が必要になった場合（例：10,000 件テスト自動化の A/B 確定、
wasm のメモリ上限に絡む UI 警告、ステージング公開設定など）はコードを書く
前にユーザーへ確認してください。

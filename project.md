# このプロジェクトの要旨

## Goal（目的）

- 選択した動画をモノクロのアニメーションGIF形式に変換しダウンロードできるWebサイト
- 変換処理はすべてユーザーのブラウザ内で完結する（サーバへ動画を送信しない）
- 元動画とアニメーションGIFのサンプルイメージは**サンプル動画**の項目を参照

## 技術スタック

- **アーキテクチャ**：純粋な静的 SPA。バックエンドサーバを持たず、変換まで
  すべての処理をブラウザ内で実行する
- **フロントエンド**：Vite + React
  - スライダ・数値入力・ドラッグ&ドロップ・進捗 UI を組みやすい
  - ビルド成果物（HTML / JS / wasm）を静的ホスティングで配信
- **変換エンジン**：[ffmpeg.wasm](https://ffmpegwasm.netlify.app/)（WebAssembly 版 ffmpeg）
  - ネイティブ ffmpeg と同じフィルタ DSL を使えるため、`scale=W:trunc(ow/dar/2)*2,hue=s=0`
    といったコマンドがそのまま流用できる
  - 配信先により実行モードを切り替える：
    - **マルチスレッド版**（高速）：`SharedArrayBuffer` を使うため COOP/COEP
      ヘッダが必要 → XServer 本番で利用
    - **シングルスレッド版**（互換重視・概ね 1/2〜1/4 速度）：ヘッダ不要
      → GitHub Pages ステージングで利用
- **本番配信**：[XServer](https://www.xserver.ne.jp/)（既存契約のレンタルサーバ）。
  既存ドメインの `/monorize/` サブパス配下に配置。`.htaccess` で COOP/COEP を
  設定し、ffmpeg.wasm のマルチスレッド版を有効化する
- **ステージング配信**：[GitHub Pages](https://pages.github.com/)（無料）。
  動作確認用。COOP/COEP を設定できないためシングルスレッド版に限定
- **ストレージ**：なし。動画・GIF はブラウザのメモリ上にのみ存在し、
  サーバへ送信・保存されることはない

## Constrains（制約）

### 入力動画

- 推奨上限：500 MB。実際の上限はブラウザ／端末のメモリ次第
  （500 MB の動画は wasm 上で展開すると 1〜2 GB のメモリを使用する）
- 形式：.mp4, .mov, .avi, .mkv, .webm, .wmv, .flv, .m4v, .ts, .3gp
- 1ファイルに対応

### UI

- 動画の選択は、ファイル選択ボタンおよびドラッグ＆ドロップに対応
  （ブラウザに読み込むのみ。サーバへ送信しない）
- 出力パラメータをユーザが指定可能
  - 横幅・高さ：スライダまたは数値入力（ピクセル数または元動画比の％）
    - レンジ：下限 32 px（または 1％）、上限は元動画の実サイズ（または 100％）
    - アップスケールは不可（出力サイズによるファイル肥大化を防ぐ）
  - フレームレート：スライダまたは数値入力
    - レンジ：1〜30 fps
  - 縦横比は元動画にロックする。横幅または高さの一方を変更すると、他方は
    元動画の縦横比から自動決定される
  - 既定値は元動画のサイズ・フレームレートとする
- 出力 GIF の推定ファイルサイズを変換前にユーザへ表示し、閾値（既定 200 MB）
  を超えそうな場合は警告を出す。ユーザが了承した場合のみ変換を実行する
  （ハード上限は設けない）
- 変換進捗をプログレスバーで表示する（ffmpeg.wasm の `progress` イベントを使用）
- 変換後にダウンロードボタンが表示されダウンロード可能となる
  （ブラウザ内で生成した Blob を `URL.createObjectURL` で配布）

### 変換エンジン

- ブラウザ内で動作する **ffmpeg.wasm** を使用する
- 出力はモノクロのアニメーションGIF
- フィルタ／引数の組み立て例（横幅 640px・15FPS の場合）

  ```
  ffmpeg -i input.mp4 -vf "scale=640:trunc(ow/dar/2)*2,hue=s=0" -r 15 output.gif
  ```

  - `scale=640:trunc(ow/dar/2)*2` — 高さを元動画のアスペクト比に合わせつつ偶数ピクセルへ丸める。`scale=640:-1` だと動画の種類によっては縦長になる場合があるため使わない
  - `hue=s=0` — 彩度ゼロでモノクロ化
  - `-r 15` — 出力フレームレート

### ファイルの取扱い

- 入力動画も出力 GIF もサーバには送信されず、ブラウザのメモリ上にのみ存在する
- ユーザーがタブを閉じる、ページをリロードするなどでブラウザメモリが解放
  された時点でファイルは消滅する
- サーバ側に保持期限・自動削除処理を実装する必要はない

## Acceptance Criteria（完了条件）

1. ローカル開発環境（`vite dev`）で手動テストに合格する
2. ローカル自動化テストで 10000 件の変換を回し、生成された GIF の品質
   チェックに合格する
3. XServer 本番環境（`既存ドメイン/monorize/`）にデプロイし、実機ブラウザで
   手動の動作確認に合格する
4. GitHub Pages ステージング環境でも動作確認に合格する
   （シングルスレッド版での挙動確認）

### 10000 件テストの実施方法

- 完全にローカルで実行（サーバ／ホスティングを介さない）
- 二択で進める。実装フェーズで A / B どちらを採用するか確定する：
  - **A: Playwright + ヘッドレスブラウザ** — 実環境に近いが時間がかかる
  - **B: Node.js + `@ffmpeg/ffmpeg`** — 速いがブラウザ実行時の挙動とは
    厳密には異なる

## サンプル動画

本プロジェクトでのイメージ動画のサンプルを示す
元動画とアニメGIFファイルのファイル名は対応している

- 元動画（変換前）
  - @sample/MAH03703.MP4
  - @sample/MAH04525.MP4
  - @sample/MAH04591.MP4
  - @sample/MAH05145.MP4
  - @sample/MAH05223.MP4
  - @sample/MAH05262.MP4

- 変換後のアニメーションGIFファイル
  - @sample/MAH03703.gif
  - @sample/MAH04525.gif
  - @sample/MAH04591.gif
  - @sample/MAH05145.gif
  - @sample/MAH05223.gif
  - @sample/MAH05262.gif

## 運用コスト

> 運用コスト（既存資源で完結する見込み）は [docs/cost.md](docs/cost.md) を参照
